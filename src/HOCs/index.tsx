import * as React from 'react'

// for higher order components
const WithAntIconSpan = ({ icon }: { icon: React.ReactNode }) => <span className="anticon">{icon}</span>

export default WithAntIconSpan
